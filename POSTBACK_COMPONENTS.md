# 📦 Componentes de Postback - Documentação Completa\n\n## Visão Geral\n\nEsta documentação descreve os componentes React criados para gerenciar o fluxo de verificação YMID e postback Monetag.\n\n---\n\n## 📋 Componentes Disponíveis\n\n### 1. PostbackYmidDialog\n\n**Arquivo**: `client/src/components/PostbackYmidDialog.tsx`\n\n**Descrição**: Modal interativo que gerencia todo o fluxo de geração e verificação de YMID.\n\n#### Props\n\n```typescript\ninterface PostbackYmidDialogProps {\n  isOpen: boolean;              // Controla se o diálogo está aberto\n  onClose?: () => void;         // Callback quando fechar\n  onBotUnlocked?: () => void;   // Callback quando bot é desbloqueado\n  onError?: (error: string) => void; // Callback para erros\n}\n```\n\n#### Funcionalidades\n\n- ✅ Gera YMID automaticamente ao abrir\n- ✅ Exibe YMID com opção de copiar\n- ✅ Mostra progresso em tempo real (impressões e cliques)\n- ✅ Verifica stats a cada 5 segundos\n- ✅ Botão para abrir Young Money\n- ✅ Feedback visual com animações\n- ✅ Indica quando bot está desbloqueado\n\n#### Exemplo de Uso\n\n```typescript\nimport { PostbackYmidDialog } from \"@/components/PostbackYmidDialog\";\nimport { useState } from \"react\";\n\nfunction MyApp() {\n  const [isOpen, setIsOpen] = useState(false);\n  const [botUnlocked, setBotUnlocked] = useState(false);\n\n  return (\n    <>\n      <button onClick={() => setIsOpen(true)}>\n        Gerar YMID\n      </button>\n\n      <PostbackYmidDialog\n        isOpen={isOpen}\n        onClose={() => setIsOpen(false)}\n        onBotUnlocked={() => {\n          setBotUnlocked(true);\n          setIsOpen(false);\n        }}\n        onError={(error) => console.error(error)}\n      />\n    </>\n  );\n}\n```\n\n#### Visual\n\n```\n┌─────────────────────────────────────────┐\n│ VoaPix Bot                          [✕] │\n│ Verificação de Tarefas                  │\n├─────────────────────────────────────────┤\n│                                         │\n│ ┌─────────────────────────────────────┐ │\n│ │ ID Único                            │ │\n│ │ YMID_1710862345_abc123              │ │\n│ │ [📋 Copiar Código]                  │ │\n│ └─────────────────────────────────────┘ │\n│                                         │\n│ Progresso: 50%                          │\n│ ✓ Impressões: 20/20                     │\n│ ○ Cliques: 1/2                          │\n│                                         │\n│ ┌─────────────────────────────────────┐ │\n│ │ ✓ Verificação Completa!             │ │\n│ │ Suas tarefas foram confirmadas       │ │\n│ └─────────────────────────────────────┘ │\n│                                         │\n│ [🚀 Acessar Young Money] [✓ Começar]    │\n│                                         │\n└─────────────────────────────────────────┘\n```\n\n---\n\n### 2. PostbackVerificationPanel\n\n**Arquivo**: `client/src/components/PostbackVerificationPanel.tsx`\n\n**Descrição**: Painel que mostra status em tempo real da API Monetag com logs detalhados.\n\n#### Props\n\n```typescript\ninterface PostbackVerificationPanelProps {\n  ymid?: string;      // YMID para exibir (opcional)\n  showLogs?: boolean; // Mostrar histórico de verificações (padrão: true)\n}\n```\n\n#### Funcionalidades\n\n- ✅ Status da API (Conectado, Verificando, Erro)\n- ✅ Hora da última verificação\n- ✅ Resumo de stats (impressões, cliques, contagem de verificações)\n- ✅ Histórico de verificações com timestamps\n- ✅ Indicadores de sucesso/erro\n- ✅ Scroll automático para logs mais recentes\n\n#### Exemplo de Uso\n\n```typescript\nimport { PostbackVerificationPanel } from \"@/components/PostbackVerificationPanel\";\n\nfunction Dashboard() {\n  return (\n    <div>\n      <h1>Painel de Verificação</h1>\n      <PostbackVerificationPanel showLogs={true} />\n    </div>\n  );\n}\n```\n\n#### Visual\n\n```\n┌─────────────────────────────────────────┐\n│ API Status        │ Última Verificação  │\n│ ● Conectado       │ 14:35:22            │\n├─────────────────────────────────────────┤\n│ Impressões: 20    │ Cliques: 2          │\n│ mín: 20           │ mín: 2              │\n├─────────────────────────────────────────┤\n│ 📋 Histórico de Verificações            │\n├─────────────────────────────────────────┤\n│ ✓ 14:35:22 Bot desbloqueado!            │\n│ → 14:35:17 Verificação #5: API OK       │\n│ → 14:35:12 Verificação #4: API OK       │\n│ → 14:35:07 Verificação #3: API OK       │\n│ → 14:35:02 Verificação #2: API OK       │\n│ → 14:34:57 Verificação #1: API OK       │\n└─────────────────────────────────────────┘\n```\n\n---\n\n### 3. PostbackYmidScreen\n\n**Arquivo**: `client/src/components/PostbackYmidScreen.tsx`\n\n**Descrição**: Tela fullscreen para verificação YMID (alternativa ao diálogo).\n\n#### Props\n\n```typescript\ninterface PostbackYmidScreenProps {\n  onBotUnlocked?: () => void;\n  onError?: (error: string) => void;\n}\n```\n\n#### Funcionalidades\n\n- ✅ Tela fullscreen com fundo animado\n- ✅ Mesmo fluxo do diálogo\n- ✅ Melhor para mobile\n- ✅ Sem opção de fechar (força conclusão)\n\n#### Exemplo de Uso\n\n```typescript\nimport { PostbackYmidScreen } from \"@/components/PostbackYmidScreen\";\nimport { useState } from \"react\";\n\nfunction App() {\n  const [showBot, setShowBot] = useState(false);\n\n  return (\n    <>\n      {!showBot ? (\n        <PostbackYmidScreen\n          onBotUnlocked={() => setShowBot(true)}\n          onError={(error) => console.error(error)}\n        />\n      ) : (\n        <BotInterface />\n      )}\n    </>\n  );\n}\n```\n\n---\n\n### 4. PostbackExample\n\n**Arquivo**: `client/src/components/PostbackExample.tsx`\n\n**Descrição**: Exemplo completo de integração mostrando como usar todos os componentes.\n\n#### Funcionalidades\n\n- ✅ Demonstra fluxo completo\n- ✅ Mostra painel de verificação\n- ✅ Inclui guia de como funciona\n- ✅ Exibe requisitos\n- ✅ Tratamento de erros\n\n#### Exemplo de Uso\n\n```typescript\nimport { PostbackExample } from \"@/components/PostbackExample\";\n\nfunction App() {\n  return <PostbackExample />;\n}\n```\n\n---\n\n## 🔄 Fluxo de Dados\n\n```\n┌──────────────────────────────────────────────────────────┐\n│ PostbackYmidDialog (Componente)                          │\n├──────────────────────────────────────────────────────────┤\n│                                                          │\n│  1. usePostback() - Hook gerenciador                    │\n│     ├── generateYmid()                                  │\n│     ├── checkStats() - Polling 5s                       │\n│     ├── checkReset() - Polling 30s                      │\n│     └── Estado: ymid, botUnlocked, stats, error         │\n│                                                          │\n│  2. Renderizar YMID                                     │\n│     ├── Copiar código                                   │\n│     └── Botão Young Money                               │\n│                                                          │\n│  3. Mostrar Progresso                                   │\n│     ├── Impressões: 0/20                                │\n│     └── Cliques: 0/2                                    │\n│                                                          │\n│  4. Verificação Automática                              │\n│     ├── A cada 5s: checkStats()                         │\n│     ├── Atualizar stats                                 │\n│     └── Se botUnlocked: onBotUnlocked()                │\n│                                                          │\n└──────────────────────────────────────────────────────────┘\n```\n\n---\n\n## 🎨 Customização\n\n### Cores\n\nOs componentes usam cores do Tailwind CSS:\n\n```typescript\n// Primária\nfrom-indigo-500 to-cyan-500\n\n// Sucesso\nbg-emerald-500\n\n// Erro\nbg-red-500\n\n// Fundo\nbg-slate-800/50\n```\n\n### Tamanhos\n\n```typescript\n// Dialog\nmax-w-md w-full\n\n// Screen\nfixed inset-0\n\n// Componentes\np-6 rounded-lg\n```\n\n### Animações\n\n```typescript\n// Fade in\nfadeIn 0.8s cubic-bezier(0.16,1,0.3,1)\n\n// Pulse\nanimate-pulse\n\n// Spin\nanimate-spin\n```\n\n---\n\n## 🔧 Integração na Aplicação\n\n### Opção 1: Usar Dialog (Recomendado)\n\n```typescript\nimport { PostbackYmidDialog } from \"@/components/PostbackYmidDialog\";\nimport { useState } from \"react\";\n\nfunction App() {\n  const [isDialogOpen, setIsDialogOpen] = useState(false);\n  const [botReady, setBotReady] = useState(false);\n\n  return (\n    <>\n      {!botReady && (\n        <button onClick={() => setIsDialogOpen(true)}>\n          Verificar Acesso\n        </button>\n      )}\n\n      <PostbackYmidDialog\n        isOpen={isDialogOpen}\n        onClose={() => setIsDialogOpen(false)}\n        onBotUnlocked={() => {\n          setBotReady(true);\n          setIsDialogOpen(false);\n        }}\n      />\n\n      {botReady && <BotScreen />}\n    </>\n  );\n}\n```\n\n### Opção 2: Usar Screen (Fullscreen)\n\n```typescript\nimport { PostbackYmidScreen } from \"@/components/PostbackYmidScreen\";\nimport { useState } from \"react\";\n\nfunction App() {\n  const [botReady, setBotReady] = useState(false);\n\n  return (\n    <>\n      {!botReady ? (\n        <PostbackYmidScreen\n          onBotUnlocked={() => setBotReady(true)}\n        />\n      ) : (\n        <BotScreen />\n      )}\n    </>\n  );\n}\n```\n\n### Opção 3: Com Painel de Verificação\n\n```typescript\nimport { PostbackYmidDialog } from \"@/components/PostbackYmidDialog\";\nimport { PostbackVerificationPanel } from \"@/components/PostbackVerificationPanel\";\nimport { useState } from \"react\";\n\nfunction App() {\n  const [isDialogOpen, setIsDialogOpen] = useState(false);\n\n  return (\n    <>\n      <button onClick={() => setIsDialogOpen(true)}>\n        Verificar\n      </button>\n\n      {isDialogOpen && <PostbackVerificationPanel showLogs={true} />}\n\n      <PostbackYmidDialog\n        isOpen={isDialogOpen}\n        onClose={() => setIsDialogOpen(false)}\n      />\n    </>\n  );\n}\n```\n\n---\n\n## 📊 Estados do Componente\n\n### PostbackYmidDialog\n\n```typescript\n// Gerando\n{\n  isGenerating: true,\n  ymid: null,\n  botUnlocked: false,\n  stats: { impressions: 0, clicks: 0 }\n}\n\n// Aguardando\n{\n  isGenerating: false,\n  ymid: \"YMID_1710862345_abc123\",\n  botUnlocked: false,\n  stats: { impressions: 5, clicks: 1 }\n}\n\n// Desbloqueado\n{\n  isGenerating: false,\n  ymid: \"YMID_1710862345_abc123\",\n  botUnlocked: true,\n  stats: { impressions: 20, clicks: 2 }\n}\n\n// Erro\n{\n  isGenerating: false,\n  ymid: null,\n  botUnlocked: false,\n  stats: { impressions: 0, clicks: 0 },\n  error: \"Erro ao gerar YMID\"\n}\n```\n\n---\n\n## ⚡ Performance\n\n### Otimizações Implementadas\n\n- ✅ useCallback para evitar re-renders desnecessários\n- ✅ useRef para gerenciar timers\n- ✅ Cleanup de timers ao desmontar\n- ✅ Debounce de atualizações de estado\n- ✅ Lazy loading de componentes\n\n### Timers\n\n```typescript\n// Verificação de stats\nCHECK_INTERVAL = 5000ms (5 segundos)\n\n// Verificação de reset\nBOT_CHECK_INTERVAL = 30000ms (30 segundos)\n\n// Feedback visual (copiar)\nsetTimeout = 2000ms (2 segundos)\n```\n\n---\n\n## 🧪 Testes\n\n### Casos de Teste\n\n```typescript\n// Teste 1: Gerar YMID\ntest(\"deve gerar YMID ao abrir diálogo\", () => {\n  // Verificar se YMID foi gerado\n});\n\n// Teste 2: Copiar código\ntest(\"deve copiar YMID para clipboard\", () => {\n  // Verificar se código foi copiado\n});\n\n// Teste 3: Verificação automática\ntest(\"deve verificar stats a cada 5 segundos\", () => {\n  // Verificar se polling está funcionando\n});\n\n// Teste 4: Desbloqueio\ntest(\"deve desbloquear bot quando requisitos são atingidos\", () => {\n  // Verificar se botUnlocked muda para true\n});\n```\n\n---\n\n## 🐛 Troubleshooting\n\n### Problema: Dialog não abre\n\n**Solução**: Verificar se `isOpen` está sendo passado corretamente\n\n```typescript\n// ❌ Errado\n<PostbackYmidDialog isOpen={true} />\n\n// ✅ Correto\nconst [isOpen, setIsOpen] = useState(false);\n<PostbackYmidDialog isOpen={isOpen} onClose={() => setIsOpen(false)} />\n```\n\n### Problema: YMID não aparece\n\n**Solução**: Verificar se hook `usePostback` está funcionando\n\n```typescript\nconst postback = usePostback();\nconsole.log(postback.ymid); // Deve ter valor\n```\n\n### Problema: Stats não atualizam\n\n**Solução**: Verificar se API Monetag está respondendo\n\n```typescript\n// Testar API manualmente\nfetch(\"https://api.monetag.com/v1/stats?ymid=YMID_xxx\")\n  .then(r => r.json())\n  .then(console.log);\n```\n\n---\n\n## 📚 Referências\n\n- [usePostback Hook](./client/src/hooks/usePostback.ts)\n- [Postback Constants](./client/src/constants/postback.ts)\n- [POSTBACK_INTEGRATION.md](./POSTBACK_INTEGRATION.md)\n- [API Monetag](https://api.monetag.com/v1/stats)\n\n
